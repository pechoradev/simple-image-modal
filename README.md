# Simple Image Modal 📸

Простая и легкая JavaScript-библиотека для автоматического открытия изображений в модальном окне. Никаких зависимостей, просто подключи и пользуйся!

## ✨ Возможности

- 🖼️ Автоматическое открытие всех изображений на странице
- 🎨 Полностью настраиваемый дизайн через CSS
- 🔍 Зум изображения (с поддержкой drag)
- ⌨️ Управление с клавиатуры (ESC для закрытия)
- 📱 Адаптивный дизайн
- ⚡ Легкая (всего 5KB в минифицированном виде)
- 🔧 Простая настройка

## ⚙️ Настройки (Options)

| Опция | Тип | По умолчанию | Описание |
|-------|-----|--------------|----------|
| `selector` | `string` | `'img'` | CSS селектор для изображений |
| `excludeSelector` | `string` | `null` | CSS селектор для исключения изображений |
| `closeOnClick` | `boolean` | `true` | Закрытие по клику на фон |
| `closeOnEsc` | `boolean` | `true` | Закрытие по клавише ESC |
| `showCaption` | `boolean` | `true` | Показывать подпись (alt или title) |
| `enableZoom` | `boolean` | `true` | Включить возможность зума |
| `maxZoom` | `number` | `3` | Максимальный коэффициент увеличения |
| `animationDuration` | `number` | `300` | Длительность анимации в мс |

## 📋 Методы API

| Метод | Параметры | Описание |
|-------|-----------|----------|
| `open(img)` | `img: HTMLImageElement` | Открывает модальное окно с указанным изображением |
| `close()` | — | Закрывает модальное окно |
| `isOpen()` | — | Проверяет, открыто ли модальное окно |
| `resetZoom()` | — | Сбрасывает масштаб изображения к исходному |
| `updateOptions(options)` | `options: Object` | Обновляет настройки экземпляра |
| `destroy()` | — | Уничтожает экземпляр и удаляет модальное окно из DOM |
